Mini Network Scanner Pro 🔐🌐

A Python-based network scanner that scans multiple ports on a target IP address, detects open services, and saves the results to a text file.

🚀 Features
Scan a range of ports on a target IP address
Detect open ports
Identify common services (HTTP, HTTPS, SSH, etc.)
Save scan results to a text file
Display the total number of open ports
